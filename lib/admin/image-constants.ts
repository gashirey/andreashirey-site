/** Longest side stored on the site (shared by server + browser compress). */
export const WEB_IMAGE_MAX_EDGE = 2400;

/** JPEG quality 0–100 (shared by server + browser compress). */
export const WEB_IMAGE_JPEG_QUALITY = 82;

/** Browser compress targets this size before upload (Vercel body limit). */
export const CLIENT_UPLOAD_MAX_MB = 2.5;
