# Fresh Andrea Shirey Supabase project URL.
NEXT_PUBLIC_SUPABASE_URL=

# Server-side only. Use Supabase Project Settings > API > secret/service role key.
SUPABASE_SERVICE_ROLE_KEY=

# Password for /admin.
ADMIN_PASSWORD=

# Optional stable signing secret for admin session cookies.
ADMIN_SESSION_SECRET=
# Supabase (contacts + inventory)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Farm manage admin (/admin) — set a strong password
ADMIN_PASSWORD=your-admin-password
# Optional: separate signing secret (defaults to ADMIN_PASSWORD)
# ADMIN_SESSION_SECRET=
